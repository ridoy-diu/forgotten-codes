#include<stdio.h>

int main()
{
    int t, n, x, y, d;

    // n = number of pages
    // x = starting page
    // y = desired page
    // d = the number of pages scrolled by pressing one button

    scanf("%d", &t);

    while(t--)
    {
        
    }

    return 0;
}